model PropertyDetail {
  id                    String   @id @default(auto()) @map("_id") @db.ObjectId
  propertyId            String   @db.ObjectId
  surface               Float
  squareMeters          Float
  frontMeters           Float
  backMeters            Float
  constructionMeters    Float
  parking               Int      @default(0)
  coveredParking        Int      @default(0)
  bedrooms              Int      @default(0)
  bathrooms             Int      @default(0)
  laundryRoom           Int      @default(0)
  serviceRoom           Int      @default(0)
  walkInCloset          Int      @default(0)
  furnished             Int      @default(0)
  fullyEquippedKitchen  Int      @default(0)
  jacuzzi               Int      @default(0)
  airConditioning       Int      @default(0)
  heating               Int      @default(0)
  garden                Int      @default(0)
  roofGarden            Int      @default(0)
  gymnasium             Int      @default(0)
  library               Int      @default(0)
  wineCellar            Int      @default(0)
  bar                   Int      @default(0)
  study                 Int      @default(0)
  tvRoom                Int      @default(0)
  livingRoom            Int      @default(0)
  diningRoom            Int      @default(0)
  gameRoom              Int      @default(0)
  pets                  Int      @default(0)
  surveillance          Int      @default(0)
  partyRoom             Int      @default(0)
  greenAreas            Int      @default(0)
  pool                  Int      @default(0)
  indoorPool            Int      @default(0)
  tennisCourt           Int      @default(0)
  golfCourse            Int      @default(0)
  playArea              Int      @default(0)
  horizontalCondo       Int      @default(0)
  clubhouse             Int      @default(0)
  terrace               Int      @default(0)
  spa                   Int      @default(0)
  laundryDryCleaning    Int      @default(0)
  helipad               Int      @default(0)
  securitySystem        Int      @default(0)
  closedCircuit         Int      @default(0)
  networkInstallation   Int      @default(0)
  elevator              Int      @default(0)
  serviceElevator       Int      @default(0)
  beach                 Int      @default(0)
  marina                Int      @default(0)
  residentialLandUse    Int      @default(0)
  residentialCommercial Int      @default(0)
  residentialOffice     Int      @default(0)
  residentialMixed      Int      @default(0)
  industry              Int      @default(0)
  trailerEntrance       Int      @default(0)
  maneuveringYard       Int      @default(0)
  warehouseArea         Int      @default(0)
  loadingArea           Int      @default(0)
  office                Int      @default(0)
  dock                  Int      @default(0)
  railway               Int      @default(0)
  officeArea            Float    @default(0)
  houseWarehouse        Int      @default(0)
  crossdock             Int      @default(0)
  warehouseHeight       Float    @default(0)
  Property              Property @relation(fields: [propertyId], references: [id])
}
